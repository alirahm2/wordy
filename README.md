# German Wordlist

Vite React app for studying the German word list.

## Local JavaScript

```sh
npm install
npm run dev
```

## Docker

```sh
docker build -t wordy .
docker run --rm -p 8080:80 wordy
```

Open http://localhost:8080.
